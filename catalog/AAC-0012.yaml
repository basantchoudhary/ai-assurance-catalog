# AAC-0012 — Model and prompt versions are pinned and rollback is exercised
# Normative. Edit here; the rendered site is generated from this file.
id: AAC-0012
legacy_id: C-12
status: active
since: "0.1.0"

level: MUST
dimension: drift
gate: false

# Applies to these archetypes. CORE cases list every archetype explicitly
# rather than relying on an inheritance keyword, so a consumer never has to
# resolve the taxonomy to know whether a case applies.
archetypes:
  - A1
  - A2
  - A3
  - A4
  - A5
  - A6
  - A7
  - A8
  - A9
  - A10
core: true

title: |-
  Model and prompt versions are pinned and rollback is exercised

statement: |-
  The exact model version and prompt revision serving each request is recorded, and reverting to the previous pair is tested rather than assumed.

# Realization is advisory: it names the class of machinery that can discharge
# the obligation, never a product. See docs/REALIZATION.md.
mechanisms:
  - M1
  - M5
stages:
  - S2
tool_class: |-
  Test runner + gateway

# Crosswalks to external frameworks live in /crosswalks, keyed by this id.
