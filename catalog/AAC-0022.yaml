# AAC-0022 — Total schema validity, including nested and optional fields
# Normative. Edit here; the rendered site is generated from this file.
id: AAC-0022
legacy_id: A2-01
status: active
since: "0.1.0"

level: MUST
dimension: format
gate: true

# Applies to these archetypes. CORE cases list every archetype explicitly
# rather than relying on an inheritance keyword, so a consumer never has to
# resolve the taxonomy to know whether a case applies.
archetypes:
  - A2
core: false

title: |-
  Total schema validity, including nested and optional fields

statement: |-
  Every response parses and validates — nested objects, arrays, optional fields and enum members. Measured as a rate, with a target of one hundred percent, because the consumer is code.

# Realization is advisory: it names the class of machinery that can discharge
# the obligation, never a product. See docs/REALIZATION.md.
mechanisms:
  - M1
stages:
  - S2
  - S4
tool_class: |-
  Test runner + gateway

# Crosswalks to external frameworks live in /crosswalks, keyed by this id.
