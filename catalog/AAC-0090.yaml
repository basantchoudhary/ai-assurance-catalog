# AAC-0090 — The judge is re-validated when it changes
# Normative. Edit here; the rendered site is generated from this file.
id: AAC-0090
legacy_id: A10-07
status: active
since: "0.1.0"

level: SHOULD
dimension: drift
gate: true

# Applies to these archetypes. CORE cases list every archetype explicitly
# rather than relying on an inheritance keyword, so a consumer never has to
# resolve the taxonomy to know whether a case applies.
archetypes:
  - A10
core: false

title: |-
  The judge is re-validated when it changes

statement: |-
  Any change to the judge model, its version or its rubric re-runs the human-agreement check before the judge is trusted again. Silently upgrading the judge invalidates every historical comparison.

# Realization is advisory: it names the class of machinery that can discharge
# the obligation, never a product. See docs/REALIZATION.md.
mechanisms:
  - M6
  - M2
stages:
  - S3
  - S6
tool_class: |-
  Annotation tool

# Crosswalks to external frameworks live in /crosswalks, keyed by this id.
