# AAC-0024 — Absent fields are null, never invented
# Normative. Edit here; the rendered site is generated from this file.
id: AAC-0024
legacy_id: A2-03
status: active
since: "0.1.0"

level: MUST
dimension: grounding
gate: true

# Applies to these archetypes. CORE cases list every archetype explicitly
# rather than relying on an inheritance keyword, so a consumer never has to
# resolve the taxonomy to know whether a case applies.
archetypes:
  - A2
core: false

title: |-
  Absent fields are null, never invented

statement: |-
  Inputs where a field is genuinely missing are tested explicitly. The model must return null rather than a plausible value — a fabricated invoice number is indistinguishable from a real one once it is in your database.

# Realization is advisory: it names the class of machinery that can discharge
# the obligation, never a product. See docs/REALIZATION.md.
mechanisms:
  - M2
  - M4
stages:
  - S3
tool_class: |-
  Eval platform

# Crosswalks to external frameworks live in /crosswalks, keyed by this id.
